'use client'

interface OnboardingTriggerProps {
  variant?: 'banner' | 'card' | 'button'
  className?: string
}

export function OnboardingTrigger(_: OnboardingTriggerProps) {
  // Onboarding prompt temporarily disabled; render nothing.
  return null
}
